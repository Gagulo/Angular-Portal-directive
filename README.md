### Directives for portal views

This can be seen as an directive that will show HTML elements if it will receive an string.
Of course it can be converted to enum so u can ezi change it in one place ;-)
